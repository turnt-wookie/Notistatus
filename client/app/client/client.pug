.container: .row: .col-md-12.text-right(style="margin-bottom:30px;")
  a.btn.btn-primary(ui-ref="client-new") Nuevo Cliente
.container: .row: .col-md-12
  table.table.table-bordered.table-hover
    thead
      tr
        th.text-center Cliente
        th.text-center Teléfono
        th.text-center Opciones
    tbody
      tr(ng-repeat="client in ctrl.clients")
        td {{client.name}}
        td {{client.phone}}
        td
          a(ui-sref="client-edit({ id: client._id })"): span.glyphicon.glyphicon-pencil
          a(ui-sref="client-view({ id: client._id })"): span.glyphicon.glyphicon-eye-open
          a(ui-sref="send-message({ id: client._id })"): span.glyphicon.glyphicon-envelope
          //-a(ui-sref=""): span.glyphicon.glyphicon-trash